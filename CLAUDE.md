# CLAUDE.md — InnerSoothe

## CRITICAL — READ FIRST

1. Read `innersoothe_project_brief__1_.md` in this directory. That is the single source of truth for all decisions, brand rules, pending items, and constraints.
2. This repo contains a **single HTML wireframe file**. All edits must be made directly to that HTML file, in place.
3. Never create .tsx, .jsx, .ts, .css, .js, or any other file format in this repo.
4. Never split content into multiple files. Everything stays in the single HTML file.
5. Never suggest or implement React Native here. This is wireframe only.
6. When asked to make any change, find the HTML file in this directory and edit it directly.
7. After every session where a decision is finalised or a change is implemented, update `innersoothe_project_brief__1_.md` to reflect it — mark resolved items as locked, move pending items to locked, update Next Steps order.
