# CLAUDE.md — InnerSoothe

## CRITICAL — READ FIRST

1. Read `context-primer.md` for a quick-start overview of the project state, locked decisions, and open questions.
2. Read `innersoothe_project_brief__1_.md` for the complete single source of truth — all decisions, brand rules, pending items, constraints, and decisions log.
3. This repo contains a **single HTML wireframe file**. All edits must be made directly to that HTML file, in place.
4. Never create .tsx, .jsx, .ts, .css, .js, or any other file format in this repo.
5. Never split content into multiple files. Everything stays in the single HTML file.
6. Never suggest or implement React Native here. This is wireframe only.
7. When asked to make any change, find the HTML file in this directory and edit it directly.
8. After every session where a decision is finalised or a change is implemented, update `innersoothe_project_brief__1_.md` to reflect it — mark resolved items as locked, move pending items to locked, update Next Steps order. Also update `context-primer.md` if the wireframe state changed.
